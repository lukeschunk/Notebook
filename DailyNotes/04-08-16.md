## Trash Icon Removed from Table

```javascript
<td>
  <Icon
    fill={Styles.Colors.PRIMARY}
    type='delete'
    style={styles.delete}
  />
</td>


this is script Object {id: "1bi8nxm", name: "*App/AFCU/*Sanity Test", description: "Sanity Test for AFCU"}

<Tree items={this.props.scripts.items} />
{obj.children.length === 0 ? 'Id:' + obj.scriptId : null}

_handleSelectDevice (deviceId) {
  // const checkBoxSelected = !this.state['isChecked' + deviceId];
  // 
  // this.setState({
  //   ['isChecked' + deviceId]: checkBoxSelected
  // });

  // if (checkBoxSelected) {
    ViewActions.selectDevice(deviceId);
  // }
},
